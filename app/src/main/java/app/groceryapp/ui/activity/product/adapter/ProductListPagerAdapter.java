package app.groceryapp.ui.activity.product.adapter;import androidx.annotation.NonNull;import androidx.annotation.Nullable;import androidx.fragment.app.Fragment;import androidx.fragment.app.FragmentManager;import androidx.fragment.app.FragmentPagerAdapter;import app.groceryapp.data.model.api.response.category.SubCategory;import app.groceryapp.ui.fragment.product.ProductListFragment;public class ProductListPagerAdapter extends FragmentPagerAdapter {    public int TAB_COUNT;    private SubCategory subCategory;    public ProductListPagerAdapter(@NonNull FragmentManager fm, int behavior, SubCategory subCategory) {        super(fm, behavior);        this.subCategory = subCategory;        // Added +1 Bcz adding 'All' as one default child category as first index        TAB_COUNT = this.subCategory.getSubSubCategoryList().size() + 1;    }    @NonNull    @Override    public Fragment getItem(int position) {        return ProductListFragment.newInstance(subCategory, position);    }    @Nullable    @Override    public CharSequence getPageTitle(int position) {        if (position == 0) {            return "All";        }        return getChildCategoryName(position - 1);    }    private String getChildCategoryName(int position) {        if(subCategory.getSubSubCategoryList().size() > position) {            return subCategory.getSubSubCategoryList().get(position).getSubSubCategoryName();        }        return "";    }    @Override    public int getCount() {       return TAB_COUNT;    }}
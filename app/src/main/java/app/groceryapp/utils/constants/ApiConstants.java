package app.groceryapp.utils.constants;import org.jetbrains.annotations.NotNull;import java.util.HashMap;import java.util.Map;import app.groceryapp.BuildConfig;import app.groceryapp.data.model.api.response.category.SubCategory;public final class ApiConstants {    private ApiConstants() {    }    public static final String API_KEY = "api_key";    public static final String PAGE    = "page"; // Used for pagination    public static final String SUB_CATEGORY_ID     = "sub_category_id";    public static final String SUB_SUB_CATEGORY_ID = "sub_sub_category_id";    @NotNull    public static Map<String, String> getBaseParams() {        Map<String, String> params =  new HashMap<>();        params.put(ApiConstants.API_KEY, BuildConfig.API_KEY);        return params;    }    public static Map<String, String> getParamsForGetProductsApi(@NotNull SubCategory subCategory, int childCategoryId, int page) {        Map<String, String> params = getBaseParams();        params.put(SUB_CATEGORY_ID, String.valueOf(subCategory.getSubCategoryId()));        params.put(SUB_SUB_CATEGORY_ID, String.valueOf(childCategoryId));        params.put(PAGE, String.valueOf(page));        return params;    }}